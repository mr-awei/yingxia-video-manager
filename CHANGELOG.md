# 更新日志（Changelog）

## v1.4.8（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 偏左对齐**：从窗口水平居中改为 `left-[40%]`，避开右侧「补齐信息」「已隐藏」「添加库」等工具栏按钮，保持清晰间距。

---

## v1.4.7（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 贴顶 + 向上阴影**：toast 进一步上移到 `top-2`，并新增向上的投影阴影，避免与窗口上边框/标题栏区域融成一片。

---

## v1.4.6（2026-08-27）· 调试增强

### 🛠 开发者工具
- **控制台快捷键**：打包版新增 `Ctrl+Shift+I` 切换开发者工具（DevTools），方便排查线上问题（开发版原本就会自动弹出）。

---

## v1.4.5（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 精确定位**：移到标题栏与主工具栏之间的空白区域，完全对齐用户红框位置。

---

## v1.4.4（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 继续上移**：从主内容区顶部中央进一步上移到紧贴标题栏下方，按箭头方向对齐。

---

## v1.4.3（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 位置优化**：从窗口正中间移到主内容区顶部中央（红框位置），既显眼又不遮挡顶部工具栏按钮。

---

## v1.4.2（2026-08-27）· UI 微调

### 🎨 界面
- **进度 toast 居中**：补齐进度提示从窗口顶部移到窗口正中间，避免与顶部工具栏/标题栏视觉冲突。
- **窗口标题精简**：标题栏由「影匣 · 海报墙」简化为「影匣」，去掉左上角「海报墙」三字。

---

## v1.4.1（2026-08-27）· Bug 修复

### 🐛 修复
- **进度条遮挡工具栏**：补齐进度条从「顶部居中浮层」改为「顶部 1px 细进度条 + 右上角百分比小字」，不抢点击（pointer-events-none），不再遮挡「补齐缺失」「已隐藏」等工具栏按钮。

---

## v1.4.0（2026-08-27）· 品牌焕新与设置增强

### 🎨 品牌焕新
- **更名「影匣」**：全新的产品名（英文/安装包/窗口标题/关于页全面更新）。

### ⚙️ 设置中心扩展
- **启动行为**：开机自启 / 启动时自动对账 / 最小化到托盘（关窗隐藏到系统托盘，托盘菜单可显示/退出）。
- **默认排序方式**：标题 / 最近添加 / 最近播放 / 评分 / 年份 / 随机。
- **隐私护盾默认开启**：启动时自动进入隐私模式（海报打码）。
- **扫描并发数**：扫描时 ffprobe 探测 / 截帧的并行任务数（1-8），快库更快。
- **海报风格**：海报密度选项升级（大图沉浸 / 标准 / 高密度）。
- **卸载入口**：设置底部「危险操作」区新增「🗑 卸载影匣」——二次确认后调用系统卸载程序，一键干净卸载。

### 🔧 其他
- 托盘图标随安装包附带（extraResources icon.png）。

---

## v1.3.1（2026-08-27）· Bug 修复

### 🐛 修复
- **补齐进度条只显示一半**：原来按「javdb/javbus/失败」三段显示在 `ok+failed` 基数上，因为 worker 只对缺数据的视频抓取（已存在的不计入 ok/failed），导致基数偏小、进度条只到一半。改为**单段连续条 = 成功 / 已处理**（成功 + 失败）；17/17 全成功时 100% 满（全绿）。

---

## v1.3.0（2026-08-27）· 细化分类适配

### 📂 简介 md 细化分类全面适配

- **8 大细化分类**：完整适配《片单_细化分类》新结构——【多人】超大规模全明星群P / 豪华哈雷姆 / 逆3P / 巴士旅行团 / 熟女人妻多人 + 【单体】同居甜蜜 / 痴女榨取 / 浓密剧情，侧栏分类墙同步更新。
- **多行结构化标签**：解析 `**标签**：` 块下的 `- **主题**/**角色**/**体型**/**服装**/**行为**/**玩法**/**场景**/**剧情**` 结构化标签，侧栏按类别分组筛选。
- **推荐评分**：md 内 `**推荐评分**` 直接作为评分展示，无需依赖 javdb。
- **空分类过滤**：文档标题 / 装饰行不再显示为分类。
- **标签兜底字典重写**：跟随新类别体系（主题/角色/体型/服装/行为/玩法/场景/剧情/版本制作），平铺标签自动归类。

---

## v1.2.0（2026-08-27）· 运行环境自适应与统计增强

### 🖥 ffmpeg 运行环境自适应

- **系统优先**：运行时按「设置指定路径 → 系统 ffmpeg（PATH/常见目录）→ 内置捆绑版」顺序查找，系统已装 ffmpeg 的电脑完全复用系统版。
- **自动释放磁盘**：检测到系统 ffmpeg 时自动删除内置捆绑版（释放 62MB），删除失败静默忽略；app 启动时自动执行，无需手动操作。
- **设置教程**：设置页新增「ffmpeg 运行环境」卡片——状态徽标（系统/手动/捆绑/缺失）、一键「重新检测」、三步图文教程（内置捆绑无需操作 / 系统自动复用 / 手动指定 gyan.dev 路径）。

### 📊 统计面板增强

- **总文件大小**：概览卡新增总文件大小（GB/TB 自适应，标注已探测部数）。
- **最大的十大文件**：新增 Top10 大文件列表（排名 + 标题 + 大小），点击直接跳转该片详情页。

### 📋 对账效率

- **一键复制未收录番号**：对账弹窗「未收录」区块新增「📋 复制所有未收录番号」按钮，一键复制全部未收录视频的番号（中文逗号间隔、自动去重），方便粘贴到简介 md 或搜索。

### 🔧 其他

- 统计面板概览卡 3 列 → 4 列布局。

---

## v1.1.0（2026-08-27）· 交互体验与数据源增强

### ⭐ 核心功能

- **随机推荐**：首页新增「随机推荐」行（每日自动换一批 + 手动「换一批」），Hero 大推荐位与随机推荐行同源联动，一键刷新同步换片；有海报的影片优先进入推荐位。
- **系列分集共享元数据**：识别常见系列后缀（-CD1/-CD2、-PART1、-DISC1、-1/-2、-A/-B），同一系列分集只抓一次信息、其余自动复用；详情页显示「属于系列 X（共 N 部）」徽章 + 同系列分集切换；按文件夹内判定，避免跨文件夹误并。
- **数据源手动切换**：设置中可选择「自动 / 仅 JavDB / 仅 JavBus」，便于单独调试数据源。
- **多数据源降级**：JavDB 抓取失败自动降级 JavBus（自动绕过年龄验证）；批量补齐时连续失败自动切源、再连续失败自动停止（阈值 3 部）。

### 🎨 界面与交互

- **数据来源标记**：卡片左上角 JavBus 琥珀色角标、详情页「数据来源」徽标，一眼区分。
- **详情页补齐信息**：单部强制重抓按钮 + 结果弹窗（成功含来源 / 失败含原因）；批量补齐汇总弹窗（成功数、来源分布、失败原因 Top3），需手动关闭。
- **大厂风格 UI 重做**：Hero 海报位（整卡点击播放、海报氛围、右上刷新）、缺封面占位符（低饱和渐变 + 圆底字母）、进度条（顶部居中浮层 + 旋转指示 + 实时进度）、批量结果卡片。
- **隐私护盾增强**：Hero 大背景图同步打码（48px 强模糊），防截图泄露。
- **移除快捷键**：删除 ⌘K 命令面板及全部组合键；保留 ESC 单键关闭（详情页/预览/弹窗）。

### 🚀 安装与体积

- **ffmpeg 捆绑进安装包**：ffmpeg 9.0.1（ffmpeg + ffprobe）直接打进安装包，新电脑装完即用视频封面截帧，零下载零配置；经 UPX 压缩（196MB → 64MB），安装包体积有效控制。
- **依赖瘦身**：react / react-dom / esbuild 移入 devDependencies，运行时安装包不再携带构建期依赖。

### 🔧 修复

- JavDB 演员解析彻底重写（zip 配对 + 去危险兜底），只保留女优、剔除男演员；支持 Big5/GBK 编码页面。
- 批量补齐按 base code 搜索（HUNTA-468CD2 → HUNTA-468），列表页/详情页封面来源同步。
- Hero / 占位符 / 按钮多处可见性与对比度修复。

---

## v1.0.0（2026-08-26）· 首个正式版

从立项到首个正式版的全部功能与迭代记录。

### 🎬 立项与核心

- **本地影库海报墙**：选择视频文件夹 + 简介 md 文件 → 按 md 分类展示海报墙，自动对账「md 条目 vs 磁盘文件」差异（缺失 / 未收录提醒）。
- **视频播放**：点击卡片查看详情，详情页「播放」用本地播放器打开；外部播放器路径可配置。
- **简介 md 权威**：分类、简介、标签、评分全部以 md 文件为唯一权威来源，重新扫描自动同步。

### 🖼 海报与图片

- **海报来源链**：手动指定 > 同名图片 > JavDB > 截帧 > 占位图，优先级可配置，本地永久缓存。
- **外文件夹参与匹配**：视频文件名带字幕组/制作组广告时，用干净的外文件夹名匹配与搜图；封面查找扩展 `poster.jpg / cover.jpg / folder.jpg / front.jpg / fanart.jpg / thumb.jpg`。
- **JavDB 封面抓取**：单部抓取（编辑弹窗）+ 批量补齐（工具栏），抓一张实时刷新一张。
- **JavDB 详情抓取**：封面、标题、日期、时长、导演、片商、系列、评分、类别、演员、关键截图；**抓一次本地永久缓存**，之后零请求秒开。
- **详情页图片本地化**：封面与关键截图全部下载到本地缓存（带 Referer + 代理），彻底规避 javdb CDN 反盗链 403。
- **代理支持**：`JavDB 代理` 设置项（HTTP/SOCKS 代理走 undici ProxyAgent），解决网络被墙无法访问 javdb.com 的问题。

### 🏷 番号与匹配

- **番号匹配规则**：大小写不敏感；版本后缀（`-uc` / `-C` / `-HD` / `1080p`）视为同片；数字边界防误匹配（`SONE-56` ≠ `SONE-560`）；同番号多版本文件全部算匹配不误报。
- **JavDB 搜索只取番号**：从标题/文件夹/文件名正则提取纯番号，绝不带描述文本污染搜索。

### 🏷 标签与评分体系

- **结构化标签**：md `**标签**：` 块 + 多行 `- **类别**：标签、...` 解析为分类标签；旧格式 `**风格**` 单行兼容。
- **分类标签侧栏**：左侧按文档分类分组展示标签 chips + 数量，多选 AND 过滤；分类可折叠。
- **分类筛选**：侧栏顶部按 md 大分类（`####` 标题）切换；所有计数联动——选中后显示「再点这个会变多少」。
- **推荐评分体系**：md `**推荐评分**` 字段提取为独立评分，覆盖 javdb 评分；首页名字条 / hover / 详情页三处展示。
- **排序与分组**：按名称 / 年份 / 评分 / 最近添加 / 最近播放 / 随机排序；分组 / 全库模式切换（适用于所有排序）。

### 🖥 UI / UX

- **四种皮肤**：🎬 影院沉浸 / ☀ 现代明亮 / 📰 杂志艺术 / 🫧 玻璃拟态，设置一键切换；海报密度（大图 / 标准 / 高密度）。
- **隐私护盾**：工具栏一键模糊所有真实海报图（`blur(26px)` 降饱和），状态持久化，截图防泄露。
- **视频详情页**：封面 + 完整元数据 + 我的评分 + 简介 + 关键截图网格（hover 原尺寸大图 lightbox，350ms 延迟防误触）。
- **Netflix 式 hover 预览**：hover 卡片停留 350ms，旁弹悬浮面板（封面 / 标题 / 评分 / 标签 / 简介 5 行），视口自动避让，点击直达详情。
- **右键菜单**：播放 / 编辑 / 打开文件位置 / 复制番号。
- **编辑弹窗重构**：大封面预览 + 分组表单（标题 / 年份 / 评分 / 标签 chips 增删 / 简介），JavDB 抓封面一键。
- **图标系统**：40+ Lucide 风格内联 SVG，全 UI 统一替换 emoji。
- **空状态 / 加载态 / 动画**：欢迎页、无结果、淡入动画、主题切换过渡、滚动条 hover 反馈。

### ⚡ 性能

- **全库虚拟滚动**（VirtualizedWall）：分类标题 + 卡片行平铺成单一虚拟列表，只渲染可见行 ± overscan——大库 DOM 恒定一屏几十张，滚动性能与库大小无关。
- **增量扫描**：对账批量写盘（一次 saveDB 代替每部全量写 JSON），未变化视频零写盘。
- **搜索防抖 200ms** + `posterUrl` 结果缓存 + 图片 `decoding=async` + `content-visibility` 跳过屏外渲染。

### 🛠 效率工具

- **批量改名**：一键清理文件名广告（`489155.com@PRED-828-C.mp4 → PRED-828-C.mp4`），预览前后对比 → 确认 → 逐个执行。
- **未收录文件定位**：对账弹窗点击文件名 → 文件管理器打开并选中，方便改名。
- **MD 自动重扫**：简介 md 文件保存即触发重新对账（fs.watch + debounce 500ms）。

### 🧹 清理

- **移除 TMDB**：全项目删除 TMDB 代码 / UI / IPC / 类型 / 文案（用户确认不再使用，后续可接入国内评分网站）。
