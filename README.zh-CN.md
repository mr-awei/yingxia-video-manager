# 影匣 YingXia

<div align="center">
  <a href="README.md">English</a>
  <span> · </span>
  <strong><a href="README.zh-CN.md">中文</a></strong>
</div>

<div align="center">
  <strong>v2.6.7</strong> ·
  <span>2026-09-02</span> ·
  <a href="CHANGELOG.md">更新日志</a>
</div>

<br />

## 本地视频海报墙管理工具 · Excel 片单驱动的私人影库

**核心原则：纯本地、不采集、不传输。**

影匣是一款 Windows 桌面应用，能把本地视频文件夹变成精美的海报墙影库。搭配一份 Excel 片单（你的个人目录）后，应用会自动整理、分类并补全元数据——所有元数据始终保存在你自己的电脑上。

> ⚠️ 本工具仅用于管理用户**本地自有**的成人视频收藏，不传播、不上传、不分享任何内容。

---

## 产品亮点

- **Excel 片单驱动**：Excel 片单是 `分类 / 推荐评分 / 简介 / 主题 / 角色 / 服装 / 体型 / 行为 / 玩法 / 场景 / 剧情 / 其他` 的唯一权威来源。随时与文件夹对账，详情页按组完整展示片单里非空的标签分组。
- **三层标签模型**：片单标签 → 结构化 `tagCategories`（分组）→ 平铺 `tags`（全部）→ 数据源 `backupTags`（JavDB/JavBus 等）。自动清洗分隔符、尾部数字、括号噪声。
- **海报墙浏览**：三种密度（沉浸 / 标准 / 紧凑），悬停放大延迟 1 秒，虚拟滚动支撑大库流畅浏览。
- **列表展示模式可设置**：可在设置页选择「全库平铺」或「按分类分组」作为默认视图，启动时自动应用。
- **智能元数据补齐**：自动识别番号，从可配置数据源（JavDB、JavBus、JavLibrary、Javapi、Javinfo）抓取元数据，自动降级；支持可拖拽重排序数据源、可暂停/继续/停止的批量抓取进度面板；每个视频的抓取失败原因同时在右下角弹窗和详情页内联展示。
- **系列分集**：同一番号多文件（如 `SONE-560_1.mp4`、`SONE-560_2.mp4`）在列表页合并为一张卡片，详情页展示分集并可切换。
- **随机截帧 + 质量过滤**：缺封面时 ffmpeg 从 12–22 个候选帧里自动跳过黑屏、白屏、模糊、单调画面；详情页自动截帧与手动「重新截帧」走同一套多帧管线。
- **统一卸载流程 + 清晰的数据选择**：设置页点击「卸载影匣」会弹出应用内确认弹窗，一步决定「保留数据」或「删除数据」，决定直接传给卸载器，不再有「先确认卸载、再问是否保留数据」的多层弹窗。删除由保护脚本把关：拒绝删除任何被你添加为媒体库的文件夹。
- **安装器第一步选语言**：NSIS 安装器首屏弹出语言选择（简体中文 / English），应用首次打开即为选定语言；卸载器也沿用该语言，设置页可随时切换。
- **英文用户须知去中国法律化**：用户须知弹窗按当前 locale 分支渲染——中文用户保留中国法律节选；英文用户展示通用免责声明，不再引用具体中国法律条文。
- **隐私护盾**：一键模糊所有封面，删除锁需 SHA-256 校验，零上传。
- **网络代理**：可配置 HTTP / HTTPS / SOCKS5 代理，同时覆盖 Node.js 请求与 Chromium 网络栈。
- **统计与发现**：按标签 / 系列 / 自定义字段筛选，查看总量、最大文件、随机推荐与收藏。
- **系统集成**：Windows NSIS 安装包、开机自启、最小化到托盘、更新检查、GitHub / Gitee 双端发布。

---

## Excel 片单结构

应用期望片单表头为**英文**，顺序如下：

| 列名 | 用途 |
| --- | --- |
| Code | 番号 / Video code（如 `SONE-560`）—— **必填** |
| Rating | 用户评分（可选） |
| Category | 分类，单值，详情页 MetaRow 独立一行展示 |
| Theme | 主题标签 |
| Role | 角色标签 |
| Costume | 服装标签 |
| BodyType | 体型标签 |
| Behavior | 行为标签 |
| Play | 玩法标签 |
| Scene | 场景标签 |
| Plot | 剧情标签 |
| Other | 其他标签 |

AI 生成的片单通常使用**中文表头**。向导页会把常见中文表头名（如 `番号`、`分类`、`标签`）自动映射到上表的英文 schema，再写入文件。

---

## 技术栈

- **桌面端**：Electron 31 + electron-vite
- **前端**：React 18 + TypeScript + Tailwind CSS
- **构建打包**：electron-builder（Windows NSIS 安装包）
- **数据**：本地 JSON + Excel（xlsx）解析
- **媒体**：ffmpeg / ffprobe（系统已装或兜底）

---

## 安装与卸载

- **下载**：从 [GitHub Releases](https://github.com/mr-awei/yingxia-video-manager/releases) 页面获取最新安装包（已镜像到 Gitee）。
- **安装**：运行 `影匣 Setup x.x.x.exe`，首屏选择界面语言，该语言会保存并在首次启动时应用。
- **卸载**：通过 Windows「应用和功能」或开始菜单入口卸载。过程中会询问**保留**还是**删除**应用数据（`%APPDATA%\local-video-manager`）。你的媒体库与媒体文件**绝不会被触碰**——删除由保护脚本把关，拒绝移除任何被你添加为媒体库的文件夹。

---

## 开发

```bash
npm install
npm run dev        # 启动开发版（自动打开 DevTools）
npm run build      # 构建渲染进程 + 主进程 + preload
npm run typecheck  # TypeScript 类型检查
npm run pack       # 清理 + 构建 + electron-builder 打安装包
```

---

## 版本历史

**v2.6.7**（2026-09-02）— 应用内卸载流程整合 · 卸载器健壮性修复

- **应用内卸载确认与保留/删除数据合并为单条流程**：设置页「卸载」弹出一个应用内确认，一步选择保留或删除，决定直接传给卸载器。
- **修复 NSIS 构建失败**（warning 6155 被当作错误处理）因多余的 `!undef`。
- **修复「无法完成用户数据清理」**：PowerShell 保护脚本原为 UTF-8 无 BOM，在中文 Windows 下被误解析；现已 UTF-8 带 BOM，正确接收 `-DataDirOverride` 参数，删除前强制结束残留进程，删除重试 5 次。

更早版本见 [CHANGELOG.md](CHANGELOG.md)。

---

## 数据隐私声明

1. **零上传**：应用不向任何服务器上传任何用户数据——包括影片列表、标签、评分、路径、文件名。
2. **本地存储**：所有数据存在 `%APPDATA%\local-video-manager\data.json`，可随时备份或删除。
3. **匿名网络请求**：仅在用户主动配置封面/数据源后，应用才会向该服务请求数据；请求不携带任何用户标识（匿名 User-Agent，不发 Cookie）。
4. **可离线使用**：完全切断网络后，除了可选的云端抓取功能外，所有核心功能（扫描、浏览、详情、播放、对账）均可正常使用。

---

## 许可证

MIT
